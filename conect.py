from flask import flask
from flask import requests
from flask import render_template

var = Flask(__name__)
def main():
	return render_template("index.html")

if __name__ == "__main__"
	var.run(host="0.0.0.0", )
	
